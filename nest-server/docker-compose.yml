version: '3'
services:
  nest:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nest_app

    restart: always
    ports:
      - 5000:${APP_PORT}
    volumes:
      - ./src:/var/app/src
